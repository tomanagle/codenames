{
    "apps": [
        {
            "name": "codenames-server",
            "script": "dist/src/app.js",
            "watch": false,
            "instances": "max",
            "exec_mode": "cluster",
            "env": {
                "NODE_ENV": "production"
            }
        }
    ]
}
